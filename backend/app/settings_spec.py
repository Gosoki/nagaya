"""配置项声明 —— 影响判定结果的东西一律放这里，不写死在代码里。

三层分法（沿用 Auto-rss2qb 的约定）：
    1. .env      只放改了必须重启的结构项（数据库连接、端口）
    2. 这张表    所有影响结果的配置，面板可改、改完即时生效
    3. 代码里    只留算法和兜底默认值

面板会按这里声明的 type / min / max 渲染控件，并把 note 原样显示出来。
"""

from __future__ import annotations

from typing import Any

SETTINGS_SPEC: dict[str, dict[str, Any]] = {
    # 这两项有自己的卡片（设置页顶上那张「应用」），所以不在通用面板里渲染。
    # 放进 SETTINGS_SPEC 是为了白拿三样东西：读写接口、类型校验、跟着备份走
    "app_name": {
        "type": "str",
        "default": "",
        "hidden": True,
        "note_zh": "这屋的 App 叫什么。加到手机主屏之后显示的就是它；留空用默认名。",
        "note_ja": "このアプリの名前。ホーム画面に追加したときに表示されます。空なら既定名。",
    },
    "app_icon_version": {
        "type": "int",
        "default": 0,
        "min": 0,
        "hidden": True,
        "note_zh": "自定义图标的版本号。0 ＝ 没设过，用打包时那个。换一次加一，"
                   "地址里带着它，好让手机和浏览器把死缓存的旧图标换掉。",
        "note_ja": "カスタムアイコンのバージョン。0 は既定アイコン。更新のたびに +1。",
    },
    "default_payer_id": {
        "type": "member_id_or_null",
        "default": None,
        "note_zh": "默认垫付人。记一笔时付款人预先填成他（我们家钱主要从一个人卡上出）。",
        "note_ja": "既定の立替者。記帳時の支払者に自動で入ります。",
    },
    "remainder_to": {
        "type": "enum",
        "default": "payer",
        "options": ["payer", "order", "rotate"],
        "note_zh": "分摊除不尽时，多出来的 1 円归谁。"
                   "payer＝给付款人（默认）／order＝按成员顺序／rotate＝逐笔轮转。"
                   "**rotate 的轮转依据是账目序号**，所以新记一笔时要等存下去才定得下来，"
                   "记账那屏的预览会说明这一点。",
        "note_ja": "割り切れないときの端数 1 円を誰が負担するか。"
                   "payer＝支払者（既定）／order＝メンバー順／rotate＝持ち回り。"
                   "**rotate は記録の連番で回す**ため、新規記帳では保存後に確定します。",
    },
    "default_rule": {
        "type": "json",
        "default": {"mode": "ratio", "equal_weight": 1},
        "note_zh": "全局默认分摊规则（第 1 层）。equal_weight 表示「所有在籍成员权重相同」，"
                   "这样加人减人都不用回来改。分类可以各自覆盖它。",
        "note_ja": "既定の分担ルール（第 1 層）。equal_weight は「在籍メンバー全員が同じ重み」の意味。"
                   "カテゴリごとに上書きできます。",
    },
    "simplify_debts": {
        "type": "bool",
        "default": True,
        "note_zh": "出账单时是否算「最少转账方案」（3 人最多 2 笔）。"
                   "副作用：可能出现原本没跟你有账的人要给你转钱。关掉则按原始债权逐笔结。",
        "note_ja": "請求書を作るときに「最小送金プラン」を計算するか（3 人なら最大 2 件）。"
                   "副作用：自分と貸し借りのなかった人から送金を受けることがあります。"
                   "オフにすると元の債権どおりに清算します。",
    },
    "fallback_category_id": {
        "type": "int_or_null",
        "default": None,
        "note_zh": "兜底分类。记支出时没点分类、但写了备注，就记到这个分类下"
                   "（默认是「其他」）。留空则不带分类照样记 —— 备注已经说清这笔是什么了。",
        "note_ja": "既定カテゴリ。カテゴリ未選択でも備考があればここに記帳します（既定は「その他」）。"
                   "空欄にするとカテゴリなしで記帳します。",
    },
    "monthly_gap_days": {
        "type": "int",
        "default": 20,
        "min": 0,
        "max": 90,
        "note_zh": "距上次出账不足这么多天时，出账单默认**不勾**「包括固定费」。"
                   "固定费一个月才收一次，隔几天再出一张多半是临时结的小账。"
                   "只改默认值，随时能自己勾回来；填 0 则永远默认勾上。",
        "note_ja": "前回の請求書作成からこの日数に満たない場合、「固定費を含める」の初期値をオフにします。"
                   "既定値を変えるだけで、手動でオンに戻せます。0 なら常にオン。",
    },
    "settlement_methods": {
        "type": "string_list",
        "default": ["现金"],
        # **还没有任何代码读它。** 记转账那一屏没有「用什么方式转的」这个字段，
        # 所以这一项现在只是个占位。摆在面板上而不说明白，等于让人以为设了就有用
        "note_zh": "结算方式候选。**还没接上**：记转账时还不能选方式，改这里暂时不影响任何事。",
        "note_ja": "精算方法の選択肢。**未接続**：送金の記帳時にまだ選べないため、変更しても今は影響しません。",
    },
    "backup_path": {
        "type": "str",
        "default": "./backups",
        # 相对路径按 backend/ 算。指到 iCloud / 外接盘那种**离开这台机器**的目录更保险：
        # 放在本机只防得住「手滑删了」和「库坏了」，防不住盘坏了
        "note_zh": "备份目录。相对路径从 backend/ 算起。每份都是一个**能单独打开**的完整库文件，"
                   "指到同步盘或外接盘才防得住盘坏。"
                   "**但那份文件是整本账的明文**（含密码哈希和头像）——"
                   "放进 iCloud 就等于「谁有那个账号谁就有整本账」，自己掂量。",
        "note_ja": "バックアップ先。相対パスは backend/ 起点。各ファイルは**単体で開ける**完全な"
                   "データベースです。ディスク故障に備えるなら同期フォルダや外付けを。"
                   "**ただし中身は台帳そのもの**（パスワードハッシュや写真を含む）——"
                   "クラウドに置くとそのアカウントを持つ人が台帳全部を持つことになります。",
    },
    "backup_every_hours": {
        "type": "int",
        "default": 24,
        "min": 0,
        "max": 24 * 30,
        # 判据是「离上一份多久了」而不是「睡够 24 小时没有」：合盖睡眠、半夜重启、
        # 改系统时间都会让后者失准，而前者读的是目录里那份文件自己的时间戳
        "note_zh": "隔多久自动备份一次。填 0 ＝ 不自动备，只留「立即备份」那个按钮。",
        "note_ja": "自動バックアップの間隔（時間）。0 なら自動では取らず、手動ボタンのみ。",
    },
    "backup_keep": {
        "type": "int",
        "default": 30,
        "min": 1,
        "max": 365,
        # 5 年规模的库实测一份 3.4MB，留 30 份约 100MB —— 家用机器不心疼，
        # 但不设上限的话它会一直涨到把盘占满
        "note_zh": "留几份。超出的按时间从最老的删起，**只删备份自己产出的文件**，"
                   "你放进这个目录的东西不动。",
        "note_ja": "保持する世代数。古いものから削除します（**バックアップが作ったファイルのみ**）。",
    },
}


def defaults() -> dict[str, Any]:
    return {k: v["default"] for k, v in SETTINGS_SPEC.items()}
