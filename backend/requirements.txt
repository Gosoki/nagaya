# 要哪些包、最低什么版本。**部署和新装开发环境都用 requirements.lock**（钉死的那一组）：
# 这份只写下限，新机器上会装到最新版，而 sqlmodel 0.0.46 起就不兼容了（见 requirements.lock 顶上）。

# Web 框架：只做 REST，不返文案（界面文字全在前端 vue-i18n，见 SPEC §7.5）
fastapi>=0.115
uvicorn[standard]>=0.30

# ORM：SQLModel = SQLAlchemy + Pydantic，和 soroban 同一套
sqlmodel>=0.0.22

# 数据库版本管理（2026-09-23 起启用，开机自动升级，见 alembic/versions/README.md）
alembic>=1.13

# 登录：JWT，token 90 天（手机登一次管仨月）
pyjwt>=2.9
bcrypt>=4.2

# 测试【不是可选的】：分摊引擎的 23 条共享用例 + 属性测试全靠它。
# httpx 是 FastAPI TestClient 的依赖，跑 API 回归要用。
pytest>=8.0
httpx>=0.27

# 头像：手机拍的照片动辄四五 MB，存进库之前要压到几 KB。
# python-multipart 是 FastAPI 收 multipart 上传的依赖。
pillow>=11.0
python-multipart>=0.0.9
