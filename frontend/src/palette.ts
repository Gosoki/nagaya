/**
 * 全站唯一的一组「可挑的颜色」。
 *
 * 成员头像、固定费分类、以后新加的分类，挑的都是这十个。
 * 分成两套的话，同一屏上会同时出现一组深色（头像）和一组亮色（分类图标），
 * 看着像两个 App 拼起来的 —— 而这两样在账单页就是上下挨着的。
 *
 * 色阶跟着品牌色 `#3d4785` 走：**深、略哑**。不是 Material 那种 300/400 的亮色 ——
 * 亮色当 30px 实心圆的底、上面压白图标时对比度不够，而固定费那一列正是这么画的。
 *
 * **和后端 `app/models.py` 的 PALETTE 是同一组**（建成员、建默认分类都要用它），
 * 改一边记得改另一边。
 */
export const PALETTE = [
  '#3d4785', // 靛（品牌色）
  '#26a69a', // 青
  '#ef6c00', // 橙
  '#c62828', // 红
  '#6a1b9a', // 紫
  '#00838f', // 蓝绿
  '#2e7d32', // 绿
  '#ad1457', // 玫红
  '#ec407a', // 粉
  '#455a64', // 蓝灰
]

/** 颜色还没拿到时的替身（成员/分类都可能短暂为空）。取色板里最中性的那个 */
export const FALLBACK = '#455a64'
