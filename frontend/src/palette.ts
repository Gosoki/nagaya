/**
 * 全站可挑的颜色。**一批色相，两个色阶** —— 不是两套色板。
 *
 * 为什么分两档，而不是统一成一档（试过，不行）：
 *   * 分类是**图标底色** —— 30px 实心圆 + 白图标，一列排五个。压到成员那一档
 *     （深、略哑）之后整块变闷，而固定费那一列是账单页最先被扫到的地方。
 *   * 成员是**身份标记** —— 头像圆点、账单每人行、分摊里谁是谁。亮一档压不住，
 *     而且它要和品牌色 `#3d4785` 待在一起。
 * 所以：分类走亮的那一档，成员走深的那一档，色相尽量对得上（靛/青/橙/红…）。
 *
 * **和后端 `app/models.py` 的两份是同一组**（建成员、建默认分类都要用），
 * 改一边记得改另一边。
 */

/** 成员头像那一档：深、略哑，和品牌色同档 */
export const MEMBER_COLORS = [
  '#3d4785', // 靛（品牌色）
  '#26a69a', // 青
  '#ef6c00', // 橙
  '#c62828', // 红
  '#6a1b9a', // 紫
  '#00838f', // 蓝绿
  '#2e7d32', // 绿
  '#ad1457', // 玫红
  '#ec407a', // 粉
  '#455a64', // 蓝灰
]

/** 分类图标那一档：亮一档的同一批色相 */
export const CATEGORY_COLORS = [
  '#5c6bc0', // 靛
  '#26a69a', // 青
  '#ffa726', // 琥珀
  '#ef5350', // 红
  '#ab47bc', // 紫
  '#29b6f6', // 天蓝
  '#66bb6a', // 绿
  '#ec407a', // 粉
  '#8d6e63', // 棕
  '#78909c', // 蓝灰
]

/** 颜色还没拿到时的替身。取最中性的那个 */
export const FALLBACK = '#90a4ae'
