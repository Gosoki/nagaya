/**
 * 全站可挑的颜色。**一组打底，另一组接着往后排** —— 不是两套色板。
 *
 * 分类那一组是**底**：界面上已经有的就是它。头像的色卡从它开始排，
 * 不够了再往后补几个深一档的色相 —— 而不是另起一套。
 *
 * **和后端 `app/models.py` 的两份是同一组**（建成员、建默认分类都要用），
 * 改一边记得改另一边（test/palette-sync.spec.ts 钉着）。
 */

/** 分类图标那一档：亮一档的同一批色相 */
export const CATEGORY_COLORS = [
  '#5c6bc0', // 靛
  '#26a69a', // 青
  '#ffa726', // 琥珀
  '#ef5350', // 红
  '#ab47bc', // 紫
  '#29b6f6', // 天蓝
  '#66bb6a', // 绿
  '#ec407a', // 粉
  '#8d6e63', // 棕
  '#78909c', // 蓝灰
]

/**
 * 头像可挑的颜色。**先用界面上已经有的那些**（就是分类那一组），
 * 不够了再往后补几个别的色相 —— 而不是另起一套色板。
 *
 * 为什么还要补：十个里有些色相会和分类撞（同一屏上「房租那个靛」和「Kan 那个靛」
 * 挨着不好认），而且三个人以上时挑得开一点更好分。补的这几个是深一档的，
 * 和品牌色 #3d4785 同档。
 */
export const MEMBER_COLORS = [
  ...CATEGORY_COLORS,
  '#3d4785', // 靛（品牌色）
  '#00838f', // 蓝绿
  '#c62828', // 红
  '#ef6c00', // 橙
  '#2e7d32', // 绿
  '#ad1457', // 玫红
]

/** 颜色还没拿到时的替身。取最中性的那个 */
export const FALLBACK = '#90a4ae'
